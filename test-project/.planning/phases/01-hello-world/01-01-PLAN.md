---
phase: 01-hello-world
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utils.js, index.js]
autonomous: true

must_haves:
  truths:
    - "Running `node index.js` prints 'Hello from GSD-RALPH!' to stdout"
    - "utils.greet('World') returns the string 'Hello, World!'"
    - "`node index.js` exits with code 0 (no errors)"
  artifacts:
    - path: "utils.js"
      provides: "greet(name) utility function"
      exports: ["greet"]
    - path: "index.js"
      provides: "Entry point that prints greeting and uses utils"
      contains: "require('./utils.js')"
  key_links:
    - from: "index.js"
      to: "utils.js"
      via: "CommonJS require"
      pattern: "require\\('./utils\\.js'\\)"
---

<objective>
Create a simple Node.js hello world application with a reusable utility module.

Purpose: Establish the project's first working code -- a utils module with a greet function and an entry point that prints output.
Output: Two JavaScript files (utils.js, index.js) that produce console output when run with `node index.js`.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hello-world/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create utils.js with greet function</name>
  <files>utils.js</files>
  <action>
    Create `utils.js` in the project root using the CommonJS `exports.greet` pattern (per research).

    The file must:
    - Export a `greet` function that takes a `name` parameter (string)
    - Return the string `Hello, {name}!` using a template literal
    - Use arrow function syntax: `exports.greet = (name) => { ... }`
    - No other exports needed for this phase

    Do NOT use `module.exports = { greet }` -- use the `exports.greet` shorthand per research recommendation to avoid mixing export patterns.
  </action>
  <verify>
    Run: `node -e "const u = require('./utils.js'); console.log(u.greet('Test'))"`
    Expected output: `Hello, Test!`
  </verify>
  <done>
    `require('./utils.js').greet('World')` returns exactly `"Hello, World!"`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create index.js entry point and verify</name>
  <files>index.js</files>
  <action>
    Create `index.js` in the project root as the application entry point.

    The file must:
    1. Require utils.js: `const utils = require('./utils.js');` (include .js extension per research)
    2. Print the main greeting: `console.log('Hello from GSD-RALPH!');`
    3. Demonstrate the greet function: `console.log(utils.greet('World'));`

    Keep it minimal -- no error handling or extra logic needed for this phase.
  </action>
  <verify>
    Run: `node index.js`
    Expected output (two lines):
    ```
    Hello from GSD-RALPH!
    Hello, World!
    ```
    Exit code must be 0.
  </verify>
  <done>
    `node index.js` prints "Hello from GSD-RALPH!" on line 1 and "Hello, World!" on line 2, exits cleanly.
  </done>
</task>

</tasks>

<verification>
1. `node -e "const u = require('./utils.js'); console.log(u.greet('Test'))"` outputs `Hello, Test!`
2. `node index.js` outputs two lines: `Hello from GSD-RALPH!` and `Hello, World!`
3. `node -e "process.exit(require('./utils.js').greet('X') === 'Hello, X!' ? 0 : 1)"` exits with code 0
4. `npm test` still passes (existing test script)
5. `npm run build` still passes (existing build script)
</verification>

<success_criteria>
- utils.js exports a working greet(name) function returning "Hello, {name}!"
- index.js prints "Hello from GSD-RALPH!" and demonstrates greet usage
- `node index.js` runs without errors
- No regressions in existing npm scripts
</success_criteria>

<output>
After completion, create `.planning/phases/01-hello-world/01-01-SUMMARY.md`
</output>
